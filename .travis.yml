group: travis_latest
language: python
matrix:
  include:
    - name: "dist: trusty"
      dist: trusty
      python: 3.6
    - name: "dist: xenial"
      dist: xenial
      python: 3.6
    - name: "os: osx"
      os: osx
    - name: "os: windows"
      language: c  # 'language: python' is not yet supported
      os: windows

install: true
script: python platform_info.py
